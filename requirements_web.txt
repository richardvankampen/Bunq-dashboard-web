# Bunq Dashboard Web - Python Backend Requirements

# Flask Framework
Flask==3.1.2
flask-cors==6.0.2
gunicorn==23.0.0

# Bunq SDK
bunq-sdk==1.28.0
requests==2.32.3  # HTTP client (Vaultwarden)

# Optional: For enhanced functionality
python-dotenv==1.2.1  # Environment variable management
flask-caching==2.3.1  # Response caching for better performance

# Development/test tooling lives in requirements_dev.txt (not installed in the Docker image).
